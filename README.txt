#AFTER running ./run script, intermediate csv files are generated
#Therefore, running the Traverse&Visualize script automatically converting all the .csv data to images(saved locally)
#In the end, running the aggregate&visualize performs the mean value calculation of all the intermediate files/models